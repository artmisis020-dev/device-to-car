# Database path (SQLite file)
SIRENA_DB=/opt/sirena-server/devices.db

# Directory where video recordings are stored
SIRENA_RECORDINGS=/opt/sirena-video/recordings

# Flask session secret — generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SIRENA_SECRET_KEY=admin_secret_key

# Admin panel password
ADMIN_PASSWORD=admin

# WebRTC upstream URL reachable from server via WireGuard
WEBRTC_PROXY_UPSTREAM=http://10.0.0.7:8092

# Timeout (seconds) for proxy requests to WebRTC upstream
WEBRTC_PROXY_TIMEOUT_S=20
